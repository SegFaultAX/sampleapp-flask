# Flask Sample App

## Getting Started

* Install:
  `pip install -r requirements.txt`

* Migrate database:
  `python migrate.py`

* Seed database:
  `python seed.py`

* Start app server:
  `python app.py`
